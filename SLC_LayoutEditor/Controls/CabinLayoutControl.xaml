<DockPanel x:Class="SLC_LayoutEditor.Controls.CabinLayoutControl"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
           xmlns:local="clr-namespace:SLC_LayoutEditor.Controls"
           mc:Ignorable="d"
           d:DesignHeight="450" d:DesignWidth="800">
    <Grid DockPanel.Dock="Top">
        <ToggleButton HorizontalAlignment="Right" Style="{DynamicResource ToggleDeckViewButtonStyle}" VerticalAlignment="Center" Visibility="Collapsed"/>
        <Border Style="{DynamicResource CardLayoutTitleStyle}" Height="55" MinWidth="460">
            <DockPanel>
                <Button DockPanel.Dock="Left" Content="{DynamicResource Template}" Click="MakeTemplate_Click"
                    ToolTip="Make a template out of this layout" Height="55" Width="55"
                    Style="{StaticResource MakeTemplateButtonStyle}" Margin="-12,-9,16,-9"/>
                <Button DockPanel.Dock="Right" Content="{DynamicResource Add}" Click="AddCabinDeck_Click"
                    ToolTip="Add a new deck to your layout" Height="55" Width="55"
                    Style="{StaticResource AddDeckButtonStyle}" Margin="16,-9,-8,-9"/>
                <Button DockPanel.Dock="Right" Content="{DynamicResource Refresh}" Click="ReloadDeck_Click"
                    ToolTip="Force the editor to reload this layout from file. Unsaved changes are lost!"
                    Margin="24,0,0,0" Style="{StaticResource RoundWarnIconButton}" Height="32" Width="32"
                    Padding="6,6,4,6"/>
                <StackPanel>
                    <TextBlock Style="{StaticResource CabinLayoutTitleStyle}" Text="Cabin layout"/>
                    <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=local:CabinLayoutControl, Mode=FindAncestor}, Path=LayoutOverviewTitle, FallbackValue=No layout selected}"
                       MinWidth="120">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource CabinLayoutNameStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=local:CabinLayoutControl, Mode=FindAncestor}, Path=HasUnsavedChanges}" Value="True">
                                        <Setter Property="FontStyle" Value="Italic"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
    <ScrollViewer x:Name="deck_scroll" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="0,8,0,0">
        <StackPanel x:Name="container_decks" Grid.IsSharedSizeScope="True" Margin="0,0,0,80"
                  SizeChanged="deck_scroll_SizeChanged">

        </StackPanel>
    </ScrollViewer>
</DockPanel>
